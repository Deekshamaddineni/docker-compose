apiVersion: apps/v1
kind: Deployment
metadata:
  name: deekshatest
  labels:
    app: 
spec:
  replicas: 2
  selector:
    matchLabels:
      app: 
  template:
    metadata:
      labels:
        app: 
    spec:
      containers:
      - name: 
        image: 
        ports:
        - containerPort: 80