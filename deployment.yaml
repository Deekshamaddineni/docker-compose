apiVersion: apps/v1
kind: Deployment
metadata:
  name: newdotnet-d-app-deployment
  namespace: deeksha
  labels:
    app: newdotnet-d-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: newdotnet-d-app
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 25%
  template:
    metadata:
      labels:
        app: newdotnet-d-app
    spec:
      initContainers:
      - name: init-setup
        image: busybox:latest
        command: ['sh', '-c', 'ls; pwd; sleep 10']
        envFrom:
        - configMapRef:
            name: newdotnet-d-app-deployment
      containers:
      - name: newdotnet-d-app
        image: hello12345.azurecr.io/newdotnet-d-app:latest
        ports:
        - containerPort: 5000
        livenessProbe:
          httpGet:
            path: /_status/healthz
            port: 5000   # Corrected port to match the containerPort
          initialDelaySeconds: 8
          periodSeconds: 10
          failureThreshold: 3  # Corrected field name
          successThreshold: 1  # Corrected field name
        readinessProbe:
          httpGet:
            path: /readiness
            port: 5000   # Corrected port to match the containerPort
          initialDelaySeconds: 15
          periodSeconds: 5
        env:
        - name: newdotnet-d-app-deployment
          valueFrom:
            configMapKeyRef:
              key: DB_HOST
              name: newdotnet-d-app-deployment
