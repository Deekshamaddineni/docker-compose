apiVersion: apps/v1
kind: Deployment
metadata:
  name: assignnew-1.0.1-deployment
  namespace: abcd
  labels:
    app: assignnew-1.0.1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: assignnew-1.0.1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 25%
  template:
    metadata:
      labels:
        app: assignnew-1.0.1
    spec:
      initContainers:
      - name: init-setup
        image: busybox:latest
        command: ['sh', '-c', 'ls; pwd; sleep 10']
        envFrom:
        - configMapRef:
            name: assignnew-1.0.1-deployment
      containers:
      - name: newdotnet-d-app
        image: hello12345.azurecr.io/assignnew-1.0.1
        ports:
        - containerPort: 5000
        livenessProbe:
          httpGet:
            path: /_status/healthz
            port: 5000   # Corrected port to match the containerPort
          initialDelaySeconds: 8
          periodSeconds: 10
          failureThreshold: 3  # Corrected field name
          successThreshold: 1  # Corrected field name
        readinessProbe:
          httpGet:
            path: /readiness
            port: 5000   # Corrected port to match the containerPort
          initialDelaySeconds: 15
          periodSeconds: 5
        env:
        - name: assignnew-1.0.1-deployment
          valueFrom:
            configMapKeyRef:
              key: DB_HOST
              name: assignnew-1.0.1-deployment
