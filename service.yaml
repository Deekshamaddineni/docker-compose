apiVersion: v1
kind: Service
metadata:
  name: hello12345-deployment 
  namespace: deeksha
spec:
  selector:
    app: newdotnet-d-app
    type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9000